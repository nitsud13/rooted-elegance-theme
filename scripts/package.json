{
  "name": "shopify-zone-populator",
  "version": "1.0.0",
  "description": "Automatically populate USDA hardiness zone metafields for Shopify products",
  "main": "populate-zones.js",
  "scripts": {
    "populate": "node populate-zones.js",
    "populate:dry-run": "node populate-zones.js --dry-run",
    "populate:verbose": "node populate-zones.js --verbose"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "node-fetch": "^3.3.2"
  }
}
